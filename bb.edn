{:tasks
 {deploy
  {:doc "Refuse local publishing; the guarded release workflow is the only publisher."
   :task
   (do
     (println "Refusing local deploy: push a v* release tag and let the guarded release workflow publish to Clojars.")
     (System/exit 1))}}}
